apiVersion: 1

providers:
  - name: IPC Benchmarks
    orgId: 1
    folder: IPC Benchmarks
    type: file
    disableDeletion: true
    editable: true
    allowUiUpdates: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false